NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Set to "true" only when running the local FastAPI backend.
# Default (unset/false) uses the embedded synthetic lab snapshot (Vercel-safe).
NEXT_PUBLIC_USE_API=false
